# 🦖 InstallProtect v1 (Anti Delete Server)

Script ini memasang proteksi agar server tidak bisa dihapus sembarangan di panel Pterodactyl.

## 🚀 Install
Jalankan sebagai root:

```bash
curl -fsSL https://raw.githubusercontent.com/aretaazhar05/install-v1.sh-by-official/main/install-V1.sh | sudo bash
